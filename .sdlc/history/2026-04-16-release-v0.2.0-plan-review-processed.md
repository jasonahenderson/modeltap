# 2026-04-16 — v0.2.0 plan review processed

> [!NOTE]
> Historical terminology: this artifact uses the former `BFF` name. The live architecture renamed that component to the `runtime server` in ADR-0016 (`.sdlc/adr/0016-runtime-server-and-client-surfaces.md`); live source now uses `internal/runtime` and the `runtime` config namespace.

## Summary

Processed the two plan reviews for v0.2.0 and applied the recommended changes
to the release planning artifacts under `.sdlc/releases/v0.2.0/`.

## Reviews Processed

- `.sdlc/releases/v0.2.0/.reviews/codex-plan-review.md` — 5 findings (4 blocking, 1 significant)
- `.sdlc/releases/v0.2.0/.reviews/kimi-plan-review.md` — 5 attention items, no blockers; overlapping findings confirmed

## Findings Addressed

| # | Finding | Resolution |
|---|---------|-----------|
| 1 | WU-049 missing protocol version negotiation + project context | Expanded WU-049 in `track-a-bff-server.md` to own tool catalog, version negotiation (with `MT-CONN-004` rejection), and project-context capture from `capabilities.register` and `session.resume` |
| 2 | WU-045 under-specified storage schema | Enumerated `sessions`, `turns`, and new `session_events` tables with all fields required by `session.list` / `session.details` (routing overrides, pinned items, compaction state, files touched/modified, server events) |
| 3 | WU-065 missing `server sessions` / `server session <id>` | Expanded WU-065 to include both CLI commands and updated the plan summary table |
| 4 | Cross-session command history has no owning WU | Added **WU-091** (Track A: `command_history` table + `history.append`/`history.list` protocol methods) and **WU-092** (Track B: BFF-sourced traversal wired into WU-070 input area) |
| 5 | Track 0 treated as universal gate | Relaxed Track 0 gate for Track B: harness-local WUs (068-072, 075-079) may start after WU-039; protocol-client/session-aware WUs still require WU-040 and WU-041. Updated `plan.md`, `track-0-shared.md`, and `track-b-terminal-harness.md` |
| 6 (Kimi) | WU-061 hardcoded 92% compaction threshold | Added `context.pressure_warning_threshold` and `context.auto_compact_threshold` config keys with the current values as defaults; definition of done now covers custom thresholds |

## Kimi Advisory Items Acknowledged (no plan changes)

- WU-060 multi-model branching — monitor during implementation; Track A already dedicates WU-060 with parallelization and cancellation in scope.
- WU-075–WU-079 tool framework scope — external deps (pdfcpu, unioffice, excelize) tracked in the individual WU work; no planning change needed.
- Cost tracking consistency between WU-056, WU-069, WU-088 — integration tests in WU-088 already assert cost accuracy end-to-end.
- MCP client timing (WU-081) — current sequencing is intentional; MCP is optional for initial harness function.

## Files Modified

- `.sdlc/releases/v0.2.0/plan.md` — Track 0 gating note, WU-049/WU-065 row edits, WU-091/WU-092 rows, summary table counts updated to 54 WUs
- `.sdlc/releases/v0.2.0/track-0-shared.md` — gating rules; WU-045 full schema enumeration including `sessions`, `turns`, `session_events`
- `.sdlc/releases/v0.2.0/track-a-bff-server.md` — WU-049 expanded; WU-061 configurable thresholds; WU-065 CLI expansion; WU-067 integration test scope; WU-091 added; WU range updated
- `.sdlc/releases/v0.2.0/track-b-terminal-harness.md` — WU-070 arrow-key note; WU-087 integration test scope; WU-092 added; WU range and gate updated
- `.sdlc/releases/v0.2.0/status.md` — totals updated to 54 WUs; gating notes revised; plan-review history section added

## New Work Units

| WU | Track | Title | Source |
|----|-------|-------|--------|
| 091 | A | Command history: storage and protocol | FEAT-0009 command history, review finding 4 |
| 092 | B | Command history: BFF-sourced traversal | FEAT-0009 command history, review finding 4 |

## Next Steps

- Planning artifacts are ready for implementation to begin at WU-039.
- No blockers remaining from the processed reviews.
- Future rounds of plan review should drop new artifacts into `.sdlc/releases/v0.2.0/.reviews/` and be processed through the same pattern.
