# 2026-05-05 — v0.3.x Design Lockdown

## Context

Closed the v0.3.x design phase across FEAT-0015..0022 by codifying the
remaining SDLC rules, committing the outstanding peer-review and
architect/SRE artifacts, absorbing all 93 findings into the feature specs,
and tagging the lockdown anchor. Implementation begins next on per-release
branches cut just-in-time from `main`.

## Discussion

User requested a clean lockdown of v0.3.x with logical commit points,
deletion of obsolete `.json` review sidecars, and a per-release branching
rule codified in SDLC docs. Five v0.3.x sub-releases (`v0.3.0` through
`v0.3.4`) already exist as planning drafts mapping to the eight features;
each has its own `plan.md`, `status.md`, `changelog.md`, plan-review
artifacts, and a scheduled ADR WU as the first work unit of the release.

Decisions reached before commits:

- Lockdown commits land on `spike/scrolling-surface-eval`, then merge to
  `main`, then release branches cut from `main`.
- Per-release branches are cut **just in time** when the release opens
  Phase 1, not at lockdown.
- Branching rule lives in `.agents/process.md`; CLAUDE.md is not
  duplicated.

## Work Completed

Five commits on `spike/scrolling-surface-eval`:

1. `EXP-0014: project-wide structural indexes beyond code` — exploration
   generalizing EXP-0012's code-graph framing to SDLC traceability,
   schemas, dependency/license, run artifacts, and multimedia transcripts,
   with a freshness-cadence design space and reference-corpora analysis
   across modeltap, meetingplaceai/alpha, and villagedata/alpha.
2. `ADMIN: codify per-release branching and markdown-only review rules` —
   adds the per-release branching section to `.agents/process.md` and
   mirrors it in `docs/releases/README.md`; aligns
   `docs/features/.reviews/README.md` with the existing markdown-only
   findings rule by removing the JSON sidecar requirement; deletes 13
   obsolete `.json` findings files from the v0.2.x and pre-architect-SRE
   v0.3.x review passes.
3. `ADMIN: add v0.3.x peer-review and architect-SRE review artifacts` —
   review inputs: 8 per-feature architect/SRE findings, the cross-feature
   continuity synthesis, the Claude id-hygiene and ownership syntheses,
   and the matching session history logs from the 2026-04-30 peer-review
   and 2026-05-04/05 architect-SRE passes.
4. `ADMIN: process v0.3.x peer-review and architect-SRE findings into
   FEAT-0015..0022` — absorbed outputs: all 93 findings (62 significant +
   31 advisory + 0 blocking) processed into umbrella commitments and
   per-feature hardening across the eight specs.
5. `ADMIN: lock down v0.3.x design` — this log + the
   `design-locked-v0.3.x` tag.

## Lockdown Anchor

Annotated git tag `design-locked-v0.3.x` placed on this commit. This is
the rollback point for v0.3.x — every release branch is cut from this tag
or from a `main` HEAD that contains it.

## Open Items

- Merge `spike/scrolling-surface-eval` into `main` with `--no-ff` so `main`
  reflects the lockdown.
- Open v0.3.0 with an `ADMIN:` commit that creates `release/v0.3.0` from
  the lockdown anchor and transitions the v0.3.0 plan from "Planning
  draft — Phase 1 not opened" to "Phase 1 — Design".
- Subsequent releases (`v0.3.1` through `v0.3.4`) stay dormant; their
  branches are cut just-in-time per the new branching rule when each one
  opens Phase 1.
- Six post-acceptance ADRs are already scheduled as the first WU of each
  release: `WU-108` (run runtime), `WU-118` (project rules / prompt
  layering), `WU-127` and `WU-128` (validation + artifact storage),
  `WU-138` (policy and workspace boundaries), `WU-147` (memory, routing,
  extension trust). These author within their owning release's Phase 1.
