# 2026-04-25 — v0.2.1 design handoff

## Current branch and worktree

- Branch: `feature/harness-shell-componentization`
- Worktree: `/Users/jasonhenderson/Projects/jasonahenderson/modeltap-harness-shell-componentization`

## Current release state

- Release: `v0.2.1`
- Current phase: **Phase 1 — Design**
- `WU-097` design is complete
- `WU-098` through `WU-102` are still pending

## What landed in this session

Transferred onto the feature branch:

- `5b010e6` `ADMIN: accept FEAT-0014 and set up v0.2.1 shell componentization release`
- `be7ce88` `SPIKE: scrolling-surface checkpoint — ▎ marker, flat surface, arrow history`
- `e23b94c` `SPIKE: move permission controls into composer`
- `db132eb` `SPIKE: simplify permission action list`
- `15e210a` `SPIKE: finish harness permission and scroll behavior`

Created:

- `docs/releases/v0.2.1/designs/2026-04-25-design-refactor-plan-097.md`
- `docs/history/2026-04-25-session-wu-097-refactor-plan.md`

Updated:

- `docs/releases/v0.2.1/plan.md`
  - marked `WU-097` design complete

## Key decisions now in force

- `FEAT-0014` is the parity target for extraction
- the existing production conversation UI is replacement scope, not the UX
  authority
- the accepted shell behavior source of truth on this branch is
  `internal/harnessspike/`
- the later `v0.2.0` harness line remains the source of command/runtime
  integration inventory for the eventual host-adapter design

## Important branch-specific constraint

This feature branch is based on an older `main` and does **not** contain the
later `internal/harness` implementation line from `v0.2.0`.

That means:

- `WU-098` can proceed using the accepted shell behavior already on this branch
- `WU-099` must explicitly inventory the later `v0.2.0` harness/runtime line
  when defining the host adapter and integration plan

## Open work

Remaining Phase 1 design artifacts:

- `WU-098` — shell component API and package-boundary design
- `WU-099` — modeltap host adapter and integration design
- `WU-100` — behavior-preserving extraction implementation design
- `WU-101` — developer docs and embedding examples design
- `WU-102` — parity and regression verification design

No Phase 2 review has happened yet.

## Recommended next step

Write `WU-098` next.

Reason:

- `WU-097` now fixes the migration strategy
- `WU-098` is the next dependency-legal design step
- `WU-099` depends on the shell boundary decisions that `WU-098` will define

## Working tree status

At handoff time, the branch has uncommitted design-doc changes:

- `docs/releases/v0.2.1/designs/2026-04-25-design-refactor-plan-097.md`
- `docs/releases/v0.2.1/plan.md`
- `docs/history/2026-04-25-session-wu-097-refactor-plan.md`
- `docs/history/2026-04-25-session-v0.2.1-design-handoff.md`

These should be committed before or alongside the next design-step commit.
