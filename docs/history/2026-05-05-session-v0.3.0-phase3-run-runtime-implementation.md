# 2026-05-05 - v0.3.0 Phase 3 Run Runtime Implementation

## Summary

Started v0.3.0 Phase 3 implementation on `release/v0.3.0`.

Implemented the durable run foundation:

- storage schema version 3 for runs, events, checkpoints, attachments,
  run-turn links, model-call accounting, and tool-result accounting
- storage API and tests for run creation, event sequencing, replay, workflow
  validation, and idempotent model-call accounting
- protocol `run.*` request/response types, run event payloads, and optional
  `run_id` on `turn.submit`
- BFF foreground-run wrapping for `turn.submit`, lifecycle events,
  checkpoints, completion/failure/cancellation updates, and run controls
- production harness routing for `/run`, `/runs`/`/jobs`, `/attach`,
  `/detach`, `/cancel`, `/retry`, `/continue`, and `/fork`
- user and embedding documentation for run commands and run projection

## Validation

`go test ./...` passed.

## Follow-up Slice

Continued the v0.3.0 runtime implementation in the same session:

- added optional `run_id` correlation to legacy `token.delta` and
  `turn.complete` payloads
- updated harness event projection to prefer durable run IDs while preserving
  turn IDs on the wire
- tightened detached-run transcript handling so mismatched run deltas do not
  mutate the foreground assistant row
- added BFF attach-conflict rejection and exact detach lease clearing
- added startup/session run recovery summaries in the production runtime

## Remaining

- full WU-116 reconnect/replay projection and detached transcript buffers
- persistent permission-blocker details
- attach replay and summary-only fidelity tests
- final v0.3.0 changelog/readiness review after all work units close
