# 2026-05-05 — v0.3.0 Phase 2 Review Processing

## Context

User requested design-review processing for `r0.3.0`, interpreted as
`v0.3.0`. Phase 1 design artifacts for WU-108 through WU-117 were already
present on `release/v0.3.0`.

## Work Completed

- Transitioned `v0.3.0` from Phase 1 design to Phase 2 review.
- Recorded the release-index/status/changelog phase update.

## Current State

Phase 2 is open. Next step in this session is recording and dispositioning the
v0.3.0 design review findings. No implementation is authorized until Phase 2
findings are processed and an explicit Phase 2 to Phase 3 `ADMIN:` transition
commit is made.

## Review Processing

Recorded Codex design review findings in
`docs/releases/v0.3.0/.reviews/codex-design-review.md` and revised the storage,
protocol, BFF runtime, harness surface, and verification designs to resolve the
findings.

Added `docs/releases/v0.3.0/designs/README.md` as the release design index for
user review and marked v0.3.0 ready for user review while keeping Phase 3
blocked.

Processed the remaining Claude design review findings F6 through F15. Revisions
added minimum observability/liveness scope, `run.create`, blocked/unblocked
events, checkpoint compatibility rules, run-accounting granularity, legal
reentry edges, `/run` subcommand stubs, sequential tool-loop policy, and
mechanical consistency fixes. Run-family budgets/deadlines/cancellation cascade
were explicitly deferred until child/sub-agent execution lands.

## Chat Handoff

User asked whether to proceed to implementation in this session or clear after
the remaining design review was processed.

Current answer: do not proceed to implementation yet. `v0.3.0` is still in
Phase 2 with Phase 3 blocked. Before implementation can begin, the release needs
accepted FEAT-0015/0016/0017 foundation scope, accepted ADR-0015, confirmed
v0.2.x prerequisite surfaces, and an explicit `ADMIN:` commit transitioning
from Phase 2 to Phase 3.

Recommended next session: process the acceptance gates and Phase 2 -> Phase 3
transition if the user approves, then begin implementation from WU dependency
order.

## Phase 3 Gate

User approved accepting the required gates so implementation can proceed for
`v0.3.0`. Accepted FEAT-0015, FEAT-0016, the FEAT-0017 foundation scope, and
ADR-0015. Confirmed the v0.2.x prerequisite protocol, BFF, harness shell,
harness host, CLI shell, and storage surfaces are present on `release/v0.3.0`.
Transitioned the release to Phase 3 implementation by explicit `ADMIN:` commit.

## Final Chat Update

User requested a chat-history update after the Phase 3 gate transition. Current
state: `release/v0.3.0` is clean, `docs/releases/v0.3.0/status.md` records
Phase 3 implementation, and the next implementation work should begin in
dependency order with WU-109/WU-110 runtime foundation tests and implementation.

## Claude Design Review Artifact (back-fill)

Earlier in the day, Claude was asked to "review release 0.3.0 design against
related features" in a fresh chat session. The cross-doc audit was delegated
to a research subagent given only the file list (six v0.3.0 design docs +
FEAT-0015/0016/0017 + plan.md/status.md) and the foundation-scope constraints.

The subagent produced a coverage matrix; Claude reconciled it against the
already-dispositioned Codex review (verifying via grep that all five Codex
dispositions had landed in the design files) and persisted the net-new
findings as `docs/releases/v0.3.0/.reviews/claude-design-review.md` (commit
`a8a06ad`).

Findings F6–F15: four significant (F6 observability axis, F7 missing
`run.create`, F8 `run.blocked` event naming, F9 fsync / N-1 compatibility) and
six advisory (run-family budgets, cost granularity, reentry edges, `/run`
subcommands, tool-loop parallelism, consistency notes). The user subsequently
dispositioned all ten — most accepted-and-applied to the designs, run-family
budgets/deadlines/cancellation cascade explicitly deferred until
child/sub-agent execution lands.

## Chat History Recording (post-Phase 3)

User requested a fresh chat-history update after manually dispositioning the
Claude design review findings, accepting the foundation scope (FEAT-0015,
FEAT-0016, FEAT-0017 foundation slice), accepting ADR-0015, and opening
Phase 3 by explicit `ADMIN:` commit `ba7ec22`.

Current state on `release/v0.3.0`:

- Phase 3 — Implementation is open.
- WU-108 (run runtime ADR) is accepted; WU-109 through WU-117 are
  "ready for implementation".
- Both Phase 2 design reviews (Codex F1-F5, Claude F6-F15) are recorded and
  fully dispositioned.
- The release branch carries 14+ commits ahead of origin and has not been
  pushed; pushing remains a discretionary user action.
- Next implementation step in dependency order: WU-109 (run schema, storage,
  migration) and WU-110 (run protocol methods and event taxonomy), since
  WU-111-113 BFF runtime and WU-114-116 harness surface depend on them.
